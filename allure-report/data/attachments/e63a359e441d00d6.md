# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ui/productTests.spec.ts >> Product details tests @ui @products >> View product details for the first product displays correct details @smoke
- Location: tests/ui/productTests.spec.ts:14:9

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('link', { name: 'Home' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('link', { name: 'Home' })
    - waiting for" https://automationexercise.com/" navigation to finish...
    - navigated to "https://automationexercise.com/"
    - waiting for navigation to finish...

```

# Test source

```ts
  1  | import { expect, Locator, Page } from "@playwright/test";
  2  | import { BasePage } from "./basePage";
  3  | import { config } from "../utils/config";
  4  | 
  5  | export class HomePage extends BasePage {
  6  | 
  7  |   private logo: Locator;
  8  |   private header: Locator;
  9  |   private consentButton: Locator;
  10 | 
  11 |   constructor(page: Page) {
  12 |     super(page);
  13 | 
  14 |     this.logo = page.getByRole('link', { name: 'Home' });
  15 |     this.header = page.locator('.item.active h1');
  16 |     this.consentButton = page.getByRole('button', { name: 'Consent' });
  17 |   }
  18 | 
  19 |   async open() {
  20 |     await this.page.goto(config.baseUrl);
  21 |   }
  22 | 
  23 |   async acceptCookiesIfPresent() {
  24 |     if (await this.consentButton.isVisible()) {
  25 |       await this.consentButton.click();
  26 |     }
  27 |   }
  28 | 
  29 |   async verifyIsAtHomePage() {
  30 |     await expect(this.page).toHaveURL(config.baseUrl);
> 31 |     await expect(this.logo).toBeVisible();
     |                             ^ Error: expect(locator).toBeVisible() failed
  32 |     await expect(this.header).toBeVisible();
  33 |     await expect(this.header).toHaveText('AutomationExercise');
  34 |   }
  35 | }
```